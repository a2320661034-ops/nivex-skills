# 树状工作流 HTML 模板

## 完整 HTML 结构

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NIVEX - {工作流名称}</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Microsoft YaHei', 'PingFang SC', 'Segoe UI', sans-serif;
            background: #f0f4f8;
            min-height: 100vh;
            padding: 40px 20px;
            color: #1e293b;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            background: #fff;
            border-radius: 20px;
            padding: 50px 40px 40px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
        }

        /* ===== HEADER ===== */
        .header { text-align: center; margin-bottom: 20px; }
        .header h1 { font-size: 26px; font-weight: 700; color: #1e40af; }
        .header .h1-vn { font-size: 16px; color: #64748b; font-weight: 400; margin-top: 4px; }
        .header .subtitle { font-size: 13px; color: #94a3b8; margin-top: 8px; }
        .header .team-size {
            display: inline-block; margin-top: 16px;
            background: linear-gradient(135deg, #1e40af, #7c3aed);
            color: #fff; padding: 10px 28px; border-radius: 10px;
            font-size: 15px; font-weight: 700;
        }
        .header .team-size .sm { font-size: 12px; font-weight: 400; opacity: 0.85; }

        /* ===== TREE LAYOUT ===== */
        .scroll-wrapper { overflow-x: auto; padding: 20px 0 30px; }
        .tree-inner { display: inline-flex; flex-direction: column; align-items: center; min-width: 100%; }
        .tree { display: flex; flex-direction: column; align-items: center; }

        /* ===== NODE STYLES ===== */
        .node {
            border-radius: 10px; padding: 14px 20px; text-align: center;
            font-weight: 600; position: relative; white-space: nowrap;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
        }
        .node .vn { display: block; font-size: 11px; font-weight: 400; opacity: 0.7; margin-top: 3px; }
        .node .detail {
            display: block; font-size: 10px; font-weight: 400; color: #64748b;
            margin-top: 4px; white-space: normal; max-width: 200px; line-height: 1.5;
        }

        /* Goal node — 目标 */
        .node.goal {
            background: linear-gradient(135deg, #1e40af, #7c3aed);
            color: #fff; font-size: 18px; padding: 18px 40px;
        }
        .node.goal .vn { color: rgba(255,255,255,0.8); font-size: 13px; }

        /* Phase nodes — 阶段（每个阶段用不同颜色） */
        .node.phase { font-size: 14px; padding: 12px 24px; color: #fff; min-width: 180px; }
        .node.phase.ph1 { background: linear-gradient(135deg, #ef4444, #dc2626); }
        .node.phase.ph2 { background: linear-gradient(135deg, #f59e0b, #d97706); }
        .node.phase.ph3 { background: linear-gradient(135deg, #10b981, #059669); }
        .node.phase.ph4 { background: linear-gradient(135deg, #3b82f6, #2563eb); }
        .node.phase.ph5 { background: linear-gradient(135deg, #8b5cf6, #7c3aed); }
        .node.phase.ph6 { background: linear-gradient(135deg, #ec4899, #db2777); }
        .node.phase.ph7 { background: linear-gradient(135deg, #14b8a6, #0d9488); }
        .node.phase.ph8 { background: linear-gradient(135deg, #f97316, #ea580c); }
        .node.phase .vn { color: rgba(255,255,255,0.8); }

        /* KPI tag inside phase node */
        .node.phase .kpi {
            display: block; font-size: 9px; font-weight: 400;
            color: rgba(255,255,255,0.75); margin-top: 4px;
            white-space: normal; max-width: 280px; line-height: 1.4;
            background: rgba(0,0,0,0.15); padding: 4px 8px; border-radius: 4px;
        }

        /* Action nodes — 具体动作 */
        .node.action {
            background: linear-gradient(135deg, #e0f2fe, #bae6fd);
            color: #0c4a6e; font-size: 12px; padding: 10px 14px;
            border: 1px solid #7dd3fc; min-width: 140px;
        }
        .node.action .vn { color: #64748b; }

        /* Person nodes — 负责人（按部门配色） */
        .node.person { font-size: 12px; padding: 8px 14px; font-weight: 500; min-width: 100px; }
        .node.person.analyst { background: linear-gradient(135deg, #dbeafe, #bfdbfe); color: #1e40af; border: 1px solid #93c5fd; }
        .node.person.content { background: linear-gradient(135deg, #fef3c7, #fde68a); color: #92400e; border: 1px solid #fbbf24; }
        .node.person.media { background: linear-gradient(135deg, #fce7f3, #fbcfe8); color: #9d174d; border: 1px solid #f472b6; }
        .node.person.cskh { background: linear-gradient(135deg, #d1fae5, #a7f3d0); color: #065f46; border: 1px solid #34d399; }
        .node.person.bd { background: linear-gradient(135deg, #fed7aa, #fdba74); color: #9a3412; border: 1px solid #fb923c; }

        /* Result node — 结果 */
        .node.result {
            background: linear-gradient(135deg, #10b981, #059669);
            color: #fff; font-size: 16px; padding: 16px 32px;
        }
        .node.result .vn { color: rgba(255,255,255,0.85); font-size: 12px; }

        /* ===== CONNECTORS ===== */
        .vline-down { width: 2px; height: 28px; background: #94a3b8; margin: 0 auto; }
        .vline-up { width: 2px; height: 28px; background: #94a3b8; }
        .vline-down.short, .vline-up.short { height: 18px; }

        /* ===== BRANCH LAYOUT ===== */
        .children { display: flex; justify-content: center; align-items: flex-start; position: relative; }
        .branch { display: flex; flex-direction: column; align-items: center; padding: 0 10px; }
        .sub-children { display: flex; justify-content: center; align-items: flex-start; }
        .sub-children > .branch { padding: 0 6px; }

        /* Arrow label — 阶段衔接标签 */
        .arrow-label { font-size: 10px; color: #94a3b8; font-weight: 600; letter-spacing: 0.5px; text-align: center; margin: 2px 0; }

        /* ===== LEGEND ===== */
        .legend {
            display: flex; flex-wrap: wrap; gap: 10px; margin-top: 30px;
            padding: 14px 20px; background: #f8fafc; border-radius: 10px;
            border: 1px solid #e2e8f0; justify-content: center; align-items: center;
        }
        .legend-title { font-size: 12px; font-weight: 600; color: #64748b; margin-right: 8px; }
        .legend-item { font-size: 11px; padding: 4px 10px; border-radius: 6px; font-weight: 600; }
        .legend-item.analyst { background: #dbeafe; color: #1e40af; }
        .legend-item.content { background: #fef3c7; color: #92400e; }
        .legend-item.media { background: #fce7f3; color: #9d174d; }
        .legend-item.bd { background: #fed7aa; color: #9a3412; }
        .legend-item.cskh { background: #d1fae5; color: #065f46; }

        .footer { text-align: center; margin-top: 24px; color: #94a3b8; font-size: 11px; }

        @media print { body { background: #fff; } .container { box-shadow: none; } }
    </style>
</head>
<body>
<div class="container">

    <!-- ===== HEADER ===== -->
    <div class="header">
        <h1>{中文标题}</h1>
        <div class="h1-vn">{越南语标题}</div>
        <div class="subtitle">NIVEX 越南 NIVEX Việt Nam | 2026年Q1</div>
        <div class="team-size">
            🎯 最少{N}人跑通全链路 <span class="sm">Tối thiểu {N} người vận hành toàn bộ</span>
        </div>
    </div>

    <div class="scroll-wrapper">
    <div class="tree-inner">
    <div class="tree">

        <!-- ===== GOAL NODE ===== -->
        <div class="node goal">
            目标：{中文目标}
            <span class="vn">Mục tiêu: {越南语目标}</span>
            <span class="kpi" style="color:rgba(255,255,255,0.85);background:rgba(0,0,0,0.2);margin-top:6px;">📊 月度总目标：{中文} | {越南语}<br>漏斗：{流量X → 线索Y → 注册Z → 转化W → 目标N}</span>
        </div>

        <div class="vline-down"></div>
        <div class="arrow-label">▼ 倒推拆解 {N}步 / Phân tích ngược {N} bước</div>
        <div class="vline-down short"></div>

        <!-- ===== PHASE N ===== -->
        <!-- 每个阶段重复以下结构 -->
        <div class="node phase ph{N}">
            ① {阶段中文名}
            <span class="vn">{阶段越南名}</span>
            <span class="kpi">📊 KPI：{中文指标} | {越南语指标}<br>⏱ {中文时间节奏} | {越南语时间节奏}</span>
        </div>

        <div class="vline-down"></div>

        <div class="children">
            <!-- 每个动作一个 branch -->
            <div class="branch">
                <div class="vline-up"></div>
                <div class="node action">
                    {动作中文名}
                    <span class="vn">{动作越南名}</span>
                    <span class="detail">{中文描述}<br>{越南语描述}</span>
                </div>
                <div class="vline-down short"></div>
                <div class="node person {部门class}">
                    {中文名} {越南名}
                    <span class="vn">{中文岗位} {越南岗位}</span>
                </div>
            </div>
        </div>

        <!-- 阶段之间的衔接 -->
        <div class="vline-down"></div>
        <div class="arrow-label">▼ {中文衔接说明} / {越南语衔接说明}</div>
        <div class="vline-down short"></div>

        <!-- ...重复更多阶段... -->

        <!-- ===== RESULT NODE ===== -->
        <div class="vline-down"></div>
        <div class="node result">
            ✅ 目标达成：{中文结果}
            <span class="vn">Đạt mục tiêu: {越南语结果}</span>
        </div>

    </div>
    </div>
    </div>

    <!-- ===== TEAM SUMMARY ===== -->
    <div style="margin-top:30px;padding:20px;background:#f8fafc;border-radius:12px;border:1px solid #e2e8f0;">
        <div style="text-align:center;font-size:14px;font-weight:700;color:#1e40af;margin-bottom:14px;">
            {N}人团队配置 Đội ngũ {N} người
        </div>
        <div style="display:flex;flex-wrap:wrap;gap:8px;justify-content:center;">
            <!-- 每个人一个 legend-item -->
            <span class="legend-item {部门class}">① {中文名} {越南名} — {中文岗位} {越南岗位}</span>
        </div>
        <div style="text-align:center;margin-top:10px;font-size:11px;color:#64748b;">
            {中文补充说明}<br>{越南语补充说明}
        </div>
    </div>

    <!-- ===== LEGEND ===== -->
    <div class="legend">
        <span class="legend-title">颜色标识 Chú thích:</span>
        <!-- 按实际用到的部门列出 -->
        <span class="legend-item analyst">分析师 Analyst</span>
        <span class="legend-item content">内容/运营 Content</span>
        <span class="legend-item media">媒体 Media</span>
        <span class="legend-item bd">商务 BD</span>
        <span class="legend-item cskh">客服 CSKH</span>
    </div>

    <div class="footer">
        NIVEX 越南 - {中文工作流名} 2026年Q1 | NIVEX Việt Nam - {越南语工作流名} Q1/2026
    </div>
</div>

<script>
// 动态绘制水平连接线
document.addEventListener('DOMContentLoaded', function() {
    function drawHBar(container) {
        const branches = Array.from(container.children).filter(el => el.classList.contains('branch'));
        if (branches.length < 2) return;
        const first = branches[0];
        const last = branches[branches.length - 1];
        const containerRect = container.getBoundingClientRect();
        const firstCenter = first.getBoundingClientRect().left + first.getBoundingClientRect().width / 2 - containerRect.left;
        const lastCenter = last.getBoundingClientRect().left + last.getBoundingClientRect().width / 2 - containerRect.left;
        let hbar = container.querySelector(':scope > .hbar-line');
        if (!hbar) {
            hbar = document.createElement('div');
            hbar.className = 'hbar-line';
            hbar.style.cssText = 'position:absolute;height:2px;background:#94a3b8;top:0;';
            container.style.position = 'relative';
            container.insertBefore(hbar, container.firstChild);
        }
        hbar.style.left = firstCenter + 'px';
        hbar.style.width = (lastCenter - firstCenter) + 'px';
    }
    function drawAll() {
        document.querySelectorAll('.children, .sub-children').forEach(drawHBar);
    }
    drawAll();
    window.addEventListener('resize', drawAll);
});
</script>
</body>
</html>
```

## 使用说明

### 节点结构规则
1. **Goal → Phases → Actions → Persons → Result** 自上而下
2. 每个 Phase 下用 `.children` 容器包裹多个 `.branch`
3. 每个 `.branch` 内：`.vline-up` → `.node.action` → `.vline-down.short` → `.node.person`
4. Phase 之间用 `.vline-down` + `.arrow-label` + `.vline-down.short` 衔接

### 双语规则
- 所有 `.node` 内主文字为中文，用 `<span class="vn">` 包裹越南语
- 所有 `.detail` 内中文在前，`<br>` 换行，越南语在后
- 所有 `.arrow-label` 格式：`▼ {中文} / {越南语}`
- Team summary 中每人格式：`{序号} {中文名} {越南名} — {中文岗位} {越南岗位}`

### Person 部门 class 对照
| 部门 | class | 说明 |
|------|-------|------|
| IP/分析 | `analyst` | 蓝色系 |
| 内容/运营 | `content` | 黄色系 |
| 媒体 | `media` | 粉色系 |
| BD/商务 | `bd` | 橙色系 |
| 客服 | `cskh` | 绿色系 |
